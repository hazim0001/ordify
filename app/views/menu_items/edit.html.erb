<div class="container mt-5 p-5 shadow bg-white" style="border-radius: 60px">
  <div class="row">
    <div class="col-8 mx-auto">
      <%= simple_form_for [@restaurant, @menu_item], url: restaurant_menu_item_path(@restaurant) do |f| %>
        <div class="row g-3">
          <div class="col-6">
            <%= f.input :title %>
          </div>
          <div class="col-6 ">
            <%= f.input :item_price %>
          </div>
        </div>
        <%= f.input :description, as: :text %>
        <%= f.input :active, as: :boolean, hint: 'Inactive to hide.', input_html: { class: "toggle" }  %>
        <%= f.input :photos, as: :file, input_html: { multiple: true }, class:"form-control" %>
        <div class="d-grid gap-2 col-6 mx-auto">
          <%= link_to 'Cancel',categories_path, class:"btn btn-outline-secondary h-25" %>
          <%= f.button :submit, class:"btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>



<!-- <div class="create-club-bg">
  <div class="container-form">
    <h1 class="text-center">Create Your Own Club </h1>
    <%#= simple_form_for Club.new do |f| %>
      <%#= f.error_notification %>
      <%#= f.input :name, placeholder: "Give an awesome name to your club" %>
      <%#= f.input :description,
        placeholder: "Describe your club to get people to join it!",
        hint: "30 characters minimum" %>
      <%#= f.input :language, as: :select, :collection => %w[Arabic Bengali English French German Hindi Japanese Javanese Korean Mandarin Portugese Russian Spanish Turkish Vietnamese], prompt: 'Select Language', label: false %>
      <%#= f.input :cover_photo, input_html: { class: 'd-none' }, label_html: { class: 'upload-photo text-dark'}, label: '📷 Upload a cover photo' %>
      <%#= f.input :private, input_html: { class: "toggle" } %>
      <%#= f.submit "Create", class: "btn btn-main" %>
    <%# end %>
  </div>
</div> -->

<style>
  label{
    font-weight: bold;
  }

  .toggle {
    position: absolute;
    transform: scale(0);
  }

  .toggle + label {
    position: relative;
    display: inline-block;
    padding-left: 40px;
  }

  .toggle + label::before {
    content: '';
    display: block;
    position: absolute;
    cursor: pointer;
    width: 36px;
    height: 20px;
    top: 0;
    left: 0;
    background-color: #EAEAEA;
    border-radius: 50rem;
  }

  .toggle:checked + label::before {
    background-color: blue;
  }

  .toggle + label::after {
    content: '';
    display: block;
    position: absolute;
    cursor: pointer;
    width: 16px;
    height: 16px;
    top: 2px;
    left: 2px;
    background-color: gray;
    border-radius: 50%;
    transition: left 0.2s ease;
  }

  .toggle:checked + label::after {
    left: 18px;
    background-color: white;
  }
</style>
